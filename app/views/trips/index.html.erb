<h1>Available Trips</h1>

<div id="trips">
  <% @trips.each do |trip| %>
  <li><%= link_to trip.destination, trip_path(trip) %></li>
  <% end %>
</div>
<br>
<% if policy(Trip).new? %>
<%= link_to "Create another trip", new_trip_path %>
<% end %>
